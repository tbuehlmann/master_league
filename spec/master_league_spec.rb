require "spec_helper"

describe MasterLeague do
  it "has a version number" do
    expect(MasterLeague::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
